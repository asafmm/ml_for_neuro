# Machine Learning for Neuroscience — Class 1

Interactive companions to Class 1 of course 1501.1027, Tel Aviv University.

Two self-contained pages, no build step and no dependencies:

- `class1_part1_what_is_ml.html` — what machine learning is
- `class1_part2_linear_regression.html` — linear regression in depth

These files are **generated**. The source lives in the course folder under
`src/`; run `python build.py` there and it rewrites the files here.
Do not edit the HTML in this repository by hand.
